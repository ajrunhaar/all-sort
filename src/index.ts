export const bubble = <T>(array: T[]): T[] => {
  return array;
};

export const binary = <T>(array: T[]): T[] => {
  return array;
};

export const heap = <T>(array: T[]): T[] => {
  return array;
};

export const quick = <T>(array: T[]): T[] => {
  return array;
};